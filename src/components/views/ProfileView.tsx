import React from "react";
import Header from '../common/Header.js'

const ProfileView = () => {
    return(
        <>
        <Header />
        <h1>Profile</h1>
        <h2>CRUD for profile goes here (when we have fixed our own auth)</h2>
        </>
    )
}

export default ProfileView